# Dashboard test runs, 2026-09-12 (three services, seven bindings)

Run from the Bazantic Recipe editor's **Test** panel with Bazantic's operator credential (no payment). Model
`anthropic/claude-opus-5`. Chain state at run time: bond 1 `Active`, coverage 595 bps, last verdict WARN at
nonce 7 (the deployed hourly monitor had advanced the nonce from 4 at 07:00 UTC). Screenshot of the first run's
tool-call log: [`docs/img/bazantic-test-kyc.jpg`](../../docs/img/bazantic-test-kyc.jpg).

## Run 1 — KYC wallet `0x8524F940EddC9EA98198Ee08071944a07C417D7b` (59,750 ms, 94,040 tokens)

| # | Tool | Arguments | Result used |
|---|---|---|---|
| 1 | Hedera Mirror Node (testnet) · `getAccount` | the address, `limit 5` | account `0.0.10455958`, 852,528,004 tinybar |
| 2 | Hedera Mirror Node (testnet) · `getTransactions` | `account.id 0.0.10455958`, `limit 5`, `order desc` | 4 transactions; newest `ETHEREUMTRANSACTION` `SUCCESS` `1789154892.654168427` id `0.0.7314364-1789154886-841148718` |
| 3 | Bond Desk API · `getWalletEligibility` | the address | `address` matched; bond 1 `canHold true` (`kyc-granted`); `hbarSufficientForGas true` |
| 4 | Bond Desk API · `listBonds` | — | bond 1 `BDB27`, `Active`, 505 bps, best ask 990000, maturity 1820569075 |
| 5 | Bank of Canada Valet · `getGroupObservations` | `bond_yields_all`, `recent 1` | `CDN.AVG.1YTO3Y.AVG` 3.30 on 2026-09-10 → 330 bps → spread **+175 bps** |
| 6 | Bond Desk API · `getBondRisk` | `id 1` | `Active`, coverage 595, `lastVerdict` WARN nonce 7 (`0xf1f6c084…dacd9`) |
| 7 | Bond Desk API · `getOrderbook` | `id 1` | ask 4 @ 990000, no bids (depth reported as not requested) |

Answer (verbatim summary): "Bond 1 (BDB27) is the single bond this wallet is both allowed to buy and worth buying:
it is KYC-granted for 0x8524F940EddC9EA98198Ee08071944a07C417D7b, its on-chain status is Active, it yields 5.05%
against a Bank of Canada 1-to-3-year marketable-bond average of 3.30% (date 2026-09-10, fresh), a spread of
+175 bps, and there is a live ask at 990000 settlement units. Disclosure: the confidential risk monitor's last
verdict is WARN (nonce 7) at coverageBps 595 (5.95%), so collateral coverage is thin even though trading is not
halted. No bond was excluded by eligibility, none was left without offers, and none failed the benchmark."

Every number matches the direct endpoints re-derived at 10:12 UTC (`recipe.md`, "Hand-derived expected result").
This output was set as the Recipe's `output_example` ("Use as output example") and is what the public catalog
returns.

## Run 2 — no-KYC wallet `0x3b44299d9F246dc775DC2e4A0B54e31DB5b31cb3` (58,072 ms, 67,212 tokens)

Required calls, in order: `getAccount` (account `0.0.10455959`, 994,818,742 tinybar) → `getTransactions`
(`account.id 0.0.10455959`; 2 transactions, newest `ETHEREUMTRANSACTION` `SUCCESS` `1789033764.684969104` id
`0.0.7314364-1789033760-568296255`, the other the funding `CRYPTOTRANSFER`) → `getWalletEligibility` (bond 1
`canHold false`, `no-kyc`).

Answer: `recommendation: null`, `eligible: false`; "No bond can be recommended … the authoritative Bond Desk
eligibility check returns canHold=false for the only registered bond (id 1, BDB27) with reason 'no-kyc'. Because
the on-chain order book rejects a non-KYC fill at the contract level, the wallet must obtain KYC from the issuer's
compliance officer before it can buy anything"; bond 1 listed under `excluded_bonds` with the reason; `next_step`
names the KYC grant on the token.

Deviation, recorded as observed: after the three required calls the model also called `listBonds`,
`getGroupObservations`, `getBondRisk` and `getOrderbook` and filed their results under "reference only"
(`stale_note: "Not applied — no eligible candidate to rank"`), instead of stopping as step 3 of the prompt says.
The answer is unaffected; the cost is four extra calls. A stricter prompt ("call no further tool") would remove
them; it was left as is so the published definition matches the one these runs exercised.

## Republish

Unpublish → Test 1 → *Use as output example* → Test 2 → **Save changes** (10:23:04 UTC) → publish through the
control MCP (`bazantic_recipe_publish`, `expected_updated_at` 2026-09-12T10:23:04.072339+00:00), then
`GET https://api.bazantic.com/v1/recipes/best-eligible-hedera-bond-recommendation` shows Run 1's answer as the
example. Listing copy for all three gateways was saved in the dashboard the same hour (`gateway.md` section 8), and
the Bank of Canada Valet listing was published to the marketplace (pending Bazantic's verification, like the Bond
Desk one).
