# Dev only. In production the app is served by the API itself and calls it same-origin.
VITE_API_URL=https://wd6nrvmajt.ap-south-1.awsapprunner.com
# VITE_RPC_URL=https://testnet.hashio.io/api
# Dev-only burner wallets (never committed): comma-separated private keys in the order investor 1, investor 2, investor 3, issuer.
# VITE_BURNER_KEYS=0x...,0x...,0x...,0x...
